
package cova.data;

/**
 * The Enum Operator for boolean operators.
 * 
 */
public enum Operator {
  /** The and operator. */
	AND, 
  /** The or operator. */
  OR,
  /** The not operator. */
  NOT,
  /** The conditional operator. */
  EQ, LT, GT, LE, GE, NE
}
